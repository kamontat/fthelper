package main

import "fmt"

var (
	name string = "ftgenerator"
	// version string = "dev"
	// commit  string = "none"
	// date    string = "unknown"
	// builtBy string = "manually"
)

func main() {
	fmt.Printf("hello %s", name)
}
